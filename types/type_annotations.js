/**
 * 	string  - for textrual data
 *	boolean - for true/false values
 *	number  - for numeric values
 *	symbol  - a unique value created by calling the Symbol constructor (unique and immutable)
 *	any     - for variables that can hold values of various types, which may be
 *		      unkown when you're writing the code
 *	unknown - a counterpart of any, but no operations are permitted on an unknown
 *			  without first asserting or narrowing it to a more speicific type
 *	never   - for representing unreachable code
 *	void    - an absence of a value
 **/
